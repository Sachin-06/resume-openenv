name: resume_screening_env
description: AI recruiter environment

# Observation returned by POST /reset and /step
observation_space:
  candidate_skills: list
  experience: int
  job_required_skills: list

# Discrete actions the agent can take
action_space:
  - shortlist
  - reject

tasks:
  - name: easy
  - name: medium
  - name: hard